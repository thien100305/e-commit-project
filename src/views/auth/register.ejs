<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng Ký - CyberStore</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>

    <header style="position: absolute; top: 0; width: 100%; background: transparent; box-shadow: none;">
        <a href="/"><h1>🎮 CyberStore</h1></a>
    </header>

    <div class="auth-body">
        <div class="auth-card" style="max-width: 450px;"> <h2>Đăng Ký Thành Viên</h2>
            <p style="color: #888; margin-bottom: 20px;">Vui lòng nhập đầy đủ thông tin!</p>

            <% if (error) { %>
                <div class="error-msg">⚠️ <%= error %></div>
            <% } %>

            <form action="/register" method="POST">
                
                <div class="form-group">
                    <label>Tên đăng nhập (*)</label>
                    <input type="text" name="username" required placeholder="Ví dụ: gameata...">
                </div>

                <div class="form-group">
                    <label>Mật khẩu (*)</label>
                    <input type="password" name="password" required placeholder="Nhập mật khẩu an toàn...">
                </div>

                <div class="form-group">
                    <label>Email (*)</label>
                    <input type="email" name="email" required placeholder="example@gmail.com">
                </div>

                <div style="display: flex; gap: 15px;">
                    <div class="form-group" style="flex: 1;">
                        <label>Số điện thoại</label>
                        <input type="text" name="phone" required placeholder="09xxxx...">
                    </div>
                    <div class="form-group" style="flex: 1;">
                        <label>Ngày sinh</label>
                        <input type="date" name="birthday" required style="color: white;"> </div>
                </div>

                <button type="submit" class="btn-submit">Tạo Tài Khoản</button>
            </form>

            <div class="auth-link">
                Đã có tài khoản rồi? <a href="/login">Đăng nhập ngay</a>
            </div>
        </div>
    </div>

</body>
</html>