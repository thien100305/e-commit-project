<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Thanh Toán - Game Store</title>
    
    <!-- Font chữ và icon Cyberpunk -->
    <link href="https://fonts.googleapis.com/css2?family=Exo+2:ital,wght@0,400;0,600;1,400&family=Orbitron:wght@500;700;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
           /* MÀU CHỦ ĐẠO + BIẾN CSS */
        :root {
            --bg-color: #050505;
            --card-bg: #111111;
            --text-color: #a0a0a0;
            --neon-primary: #fcee0a;   /* Màu vàng neon */
            --neon-secondary: #00f0ff; /* Màu xanh neon */
            --white: #ffffff;
            --input-bg: #1a1a1a;
        }

        /* NỀN TRANG + HIỆU ỨNG LƯỚI */
        body {
            font-family: 'Exo 2', sans-serif;
            background-color: var(--bg-color);
            background-image: 
                linear-gradient(rgba(0, 240, 255, 0.03) 1px, transparent 1px),
                linear-gradient(90deg, rgba(0, 240, 255, 0.03) 1px, transparent 1px);
            background-size: 30px 30px;
            color: var(--text-color);
            margin: 0;
            padding: 40px 20px;
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
        }

        /* KHUNG CHỨA FORM */
        .container {
            background-color: var(--card-bg);
            max-width: 600px;
            width: 100%;
            padding: 40px;
            border: 1px solid #333;
            box-shadow: 0 0 30px rgba(0,0,0,0.8);
            position: relative;
        }

        /* Trang trí góc Cyberpunk */
        .container::before {
            content: '';
            position: absolute;
            top: -2px; left: -2px;
            width: 30px; height: 30px;
            border-top: 2px solid var(--neon-secondary);
            border-left: 2px solid var(--neon-secondary);
        }
        .container::after {
            content: '';
            position: absolute;
            bottom: -2px; right: -2px;
            width: 30px; height: 30px;
            border-bottom: 2px solid var(--neon-secondary);
            border-right: 2px solid var(--neon-secondary);
        }

        /* TIÊU ĐỀ */
        h2 {
            font-family: 'Orbitron', sans-serif;
            text-align: center;
            color: var(--white);
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 30px;
            border-bottom: 1px dashed #333;
            padding-bottom: 20px;
        }
        h2 i { color: var(--neon-primary); margin-right: 10px; }

        /* Ô nhập liệu */
        .form-group { margin-bottom: 20px; }

        label {
            display: block;
            margin-bottom: 8px;
            font-family: 'Orbitron', sans-serif;
            font-weight: 700;
            color: var(--neon-secondary);
            font-size: 0.9em;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        input {
            width: 100%;
            padding: 12px 15px;
            background: var(--input-bg);
            border: 1px solid #333;
            color: var(--white);
            font-family: 'Exo 2', sans-serif;
            font-size: 1em;
            box-sizing: border-box;
            outline: none;
            transition: all 0.3s;
        }

        input::placeholder { color: #555; font-style: italic; }

        input:focus {
            border-color: var(--neon-secondary);
            box-shadow: 0 0 10px rgba(0, 240, 255, 0.2);
            background: #222;
        }

        /* KHUNG TỔNG TIỀN */
        .summary {
            background: rgba(0, 240, 255, 0.05);
            padding: 20px;
            border-left: 4px solid var(--neon-primary);
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .summary strong {
            font-family: 'Orbitron', sans-serif;
            color: var(--white);
        }

        .total-amount {
            font-family: 'Orbitron', sans-serif;
            color: var(--neon-primary);
            font-size: 1.5em;
            font-weight: 900;
        }

        /* NÚT XÁC NHẬN */
        .btn-submit {
            width: 100%;
            background: var(--neon-primary);
            color: #000;
            padding: 15px;
            border: none;
            font-family: 'Orbitron', sans-serif;
            font-weight: 900;
            font-size: 1.2em;
            text-transform: uppercase;
            cursor: pointer;
            clip-path: polygon(5% 0, 100% 0, 100% 80%, 95% 100%, 0 100%, 0 20%);
            transition: all 0.2s;
            margin-top: 10px;
        }

        .btn-submit:hover {
            background: var(--white);
            box-shadow: 0 0 20px var(--white);
            transform: translateY(-2px);
        }

        /* LINK QUAY LẠI */
        .back-link {
            display: block;
            margin-top: 25px;
            text-align: center;
            text-decoration: none;
            color: #666;
            font-family: 'Orbitron', sans-serif;
        }

        .back-link:hover { color: var(--neon-secondary); }

    </style>
</head>
<body>
    <div class="container">

        <!-- TIÊU ĐỀ THANH TOÁN -->
        <h2><i class="fas fa-id-card"></i> THÔNG TIN ĐẶT HÀNG</h2>
        
        <!-- HIỂN THỊ TỔNG TIỀN THANH TOÁN -->
        <div class="summary">
            <strong>Tổng thanh toán:</strong> 

            <!-- Hiển thị totalPrice, tránh lỗi nếu undefined -->
            <span class="total-amount">$<%= (totalPrice || 0).toLocaleString() %></span>
        </div>

        <!-- FORM GỬI DỮ LIỆU ĐỂ TẠO ĐƠN HÀNG -->
        <form action="/cart/checkout" method="POST">
            
            <!-- Nếu user mua nhanh Quick Buy → gửi kèm ID game -->
            <% if (typeof quickBuyId !== 'undefined') { %>
                <input type="hidden" name="quickBuyId" value="<%= quickBuyId %>">
            <% } %>

            <!-- NHẬP HỌ TÊN -->
            <div class="form-group">
                <label>Họ tên</label>
                <input type="text" name="customerName" required placeholder="Nhập tên thật để xác minh...">
            </div>
            
            <!-- NHẬP SĐT -->
            <div class="form-group">
                <label>SĐT</label>
                <input type="text" name="phone" pattern="[0-9]{9,11}" required placeholder="09xxxxxxxx...">
            </div>

            <!-- NHẬP EMAIL (nhưng name vẫn là 'address' để backend không lỗi) -->
            <div class="form-group">
                <label for="address">Email</label>
                <input type="email" id="address" name="address" placeholder="Nhập email nhận key game" required>
            </div>

            <!-- NÚT GỬI FORM -->
            <button type="submit" class="btn-submit">XÁC NHẬN ĐƠN HÀNG</button>
        </form>
        
        <!-- QUAY LẠI GIỎ HÀNG -->
        <a href="/cart" class="back-link"><i class="fas fa-caret-left"></i>Hủy và trở lại giỏ hàng</a>
    </div>
</body>
</html>
